export default async function Login({searchParams}:{searchParams:Promise<{e?:string}>}){const sp=await searchParams;return <main className="login-wrap"><div className="login-card"><h1>Login</h1><p className="muted">Default password is same as User ID. First login requires reset.</p>{sp?.e&&<div className="error">{sp.e}</div>}<form action="/api/login" method="post"><label>User ID</label><input name="user_id" required/><label>Password</label><input name="password" type="password" required/><button>Login</button></form><p className="muted small">Admin/HoD default: <b>hod / hod</b>. Faculty example: <b>shivanagowda / shivanagowda</b>.</p></div></main>}
