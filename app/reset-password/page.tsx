import { currentUser } from '@/lib/auth';import { redirect } from 'next/navigation';
export default async function Reset(){const u=await currentUser();if(!u)redirect('/login');return <><div className="topbar"><div className="brand">Password Reset</div></div><main className="wrap"><div className="card" style={{maxWidth:480,margin:'40px auto'}}><h2>Mandatory Password Reset</h2><p className="muted">Welcome {u.name}. Please change your initial password.</p><form method="post" action="/api/reset-password"><p><label>New Password</label><input type="password" name="password" required minLength={6}/></p><p><label>Confirm Password</label><input type="password" name="confirm" required minLength={6}/></p><button>Save Password</button></form></div></main></>}
