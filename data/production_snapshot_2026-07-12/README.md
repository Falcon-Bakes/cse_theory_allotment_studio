# Production data snapshot supplied for V1.4 development

This folder contains the supplied course, preference and settings exports plus a **sanitized** faculty export. Password hashes were deliberately excluded from the packaged development snapshot.

These files are for staging verification only. They are not automatically imported and must never be used to reset production.
